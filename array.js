//to run this in terminal we need to use (node array.js)
//to run we need to run (node file name).
// console.log("Joseph");

//arrays
var names = ["Joseph","Farid","Tawil"];
var numbers = [1,2,3,4,5];
var mix = [true,false,1,2,3,4,5,"Joseph",[1,2,3]];

// console.log(names);
// console.log(numbers[3]);
//to access array in array we have to add another dimension next to the big array
//to see which element of the small array you are looking for
console.log(mix[8][2]);